name: 13 - Olay Silme PR Oluşturucu

on:
  issues:
    types: [closed]

jobs:
  create-delete-pr:
    if: contains(github.event.issue.labels.*.name, 'delete-event')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Parse issue body
        id: parse
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          echo "=== DELETE ISSUE PARSE BAŞLADI ==="
          
          # Değişkenleri başlat
          FILENAME=""
          REASON=""
          
          # Issue body'yi satır satır oku
          current_section=""
          content=""
          
          while IFS= read -r line; do
            # Başlık kontrolü (### ile başlıyor mu?)
            if [[ "$line" =~ ^###[[:space:]](.+) ]]; then
              # Önceki bölümü kaydet
              if [[ -n "$current_section" ]]; then
                case "$current_section" in
                  "Dosya Adı (Zorunlu)")
                    FILENAME="$content"
                    ;;
                  "Silme Gerekçesi")
                    REASON="$content"
                    ;;
                esac
              fi
              
              # Yeni bölüm başlat
              current_section="${BASH_REMATCH[1]}"
              content=""
            elif [[ -n "$line" && ! "$line" =~ ^-[[:space:]]\[.?\] ]]; then
              # Checkbox değilse ve boş satır değilse içeriğe ekle
              if [[ -n "$content" ]]; then
                content="$content"$'\n'"$line"
              else
                content="$line"
              fi
            fi
          done <<< "$ISSUE_BODY"
          
          # Son bölümü de kaydet
          if [[ -n "$current_section" ]]; then
            case "$current_section" in
              "Dosya Adı (Zorunlu)")
                FILENAME="$content"
                ;;
              "Silme Gerekçesi")
                REASON="$content"
                ;;
            esac
          fi
          
          # Temizlik
          FILENAME=$(echo "$FILENAME" | tr -d '\r' | xargs)
          REASON=$(echo "$REASON" | tr -d '\r' | xargs)
          
          echo "Dosya adı: $FILENAME"
          echo "Silme nedeni: $REASON"
          
          # Dosya varlığını kontrol et
          if [[ ! -f "events/data/$FILENAME" ]]; then
            echo "❌ HATA: Dosya bulunamadı: events/data/$FILENAME"
            echo "file_exists=false" >> $GITHUB_OUTPUT
            echo "filename=$FILENAME" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "✅ Dosya bulundu: events/data/$FILENAME"
          echo "file_exists=true" >> $GITHUB_OUTPUT
          echo "filename=$FILENAME" >> $GITHUB_OUTPUT
          echo "reason=$REASON" >> $GITHUB_OUTPUT

      - name: Delete file and create PR
        if: steps.parse.outputs.file_exists == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "🗑️ Olay sil: ${{ steps.parse.outputs.filename }}"
          branch: event/delete-${{ github.event.issue.number }}
          delete-branch: true
          title: "🗑️ Olay Sil: ${{ steps.parse.outputs.filename }}"
          body: |
            ## ⛔ Olay Silme İsteği
            
            **Issue:** #${{ github.event.issue.number }}
            **Silinecek Dosya:** `${{ steps.parse.outputs.filename }}`
            **Silme Gerekçesi:** ${{ steps.parse.outputs.reason }}
            
            ---
            ⚠️ **DİKKAT:** Bu işlem geri alınamaz!
            
            Bu PR, issue #${{ github.event.issue.number }} (kapatıldığı için) tarafından otomatik olarak oluşturuldu.
            
            **Lütfen "Merge pull request" butonuna basarak silme işlemini onaylayın.**
          
          # Silme işlemini PR oluşturmadan önce yap
          prepare-script: |
            echo "Dosya siliniyor: events/data/${{ steps.parse.outputs.filename }}"
            if [ -f "events/data/${{ steps.parse.outputs.filename }}" ]; then
              git rm "events/data/${{ steps.parse.outputs.filename }}"
              echo "✅ Dosya silindi"
            else
              echo "❌ Dosya bulunamadı!"
              exit 1
            fi
          
          labels: |
            event
            deletion
            automated
        id: create-pr

      - name: Comment on issue - Success
        if: steps.parse.outputs.file_exists == 'true' && steps.create-pr.outputs.pull-request-number
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `✅ Issue kapatıldı. Silme işlemi için Pull Request oluşturuldu!\n\n🔗 **PR:** #${{ steps.create-pr.outputs.pull-request-number }}\n\n⚠️ **Uyarı:** PR onaylandığında olay kalıcı olarak silinecektir.`
            })

      - name: Comment on issue - File not found
        if: steps.parse.outputs.file_exists == 'false'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `❌ **HATA:** \`${{ steps.parse.outputs.filename }}\` adında bir dosya bulunamadı. Lütfen dosya adını kontrol edip issue'yu yeniden açın.`
            })
