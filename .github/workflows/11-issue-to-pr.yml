name: 11 - Olay PR Oluşturucu (JSON Format)

on:
  issues:
    types: [closed]

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    
    steps:
      - name: Check labels and skip if not supported
        id: check_labels
        run: |
          LABELS_JSON='${{ toJSON(github.event.issue.labels) }}'
          if echo "$LABELS_JSON" | grep -q '"name":"new-event"' || echo "$LABELS_JSON" | grep -q '"name":"upgrade-event"' || echo "$LABELS_JSON" | grep -q '"name":"delete-event"'; then
            echo "continue=true" >> $GITHUB_OUTPUT
          else
            echo "continue=false" >> $GITHUB_OUTPUT
          fi

      - name: Exit if not supported label
        if: steps.check_labels.outputs.continue == 'false'
        run: |
          echo "Bu issue desteklenmeyen etiket taşıyor. İşlem iptal ediliyor."
          exit 0

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Parse JSON data from issue body
        id: parse_json
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          echo "=== ISSUE BODY START ==="
          echo "$ISSUE_BODY"
          echo "=== ISSUE BODY END ==="
          
          # JSON verisini çıkar
          JSON_DATA=""
          
          # EVENT JSON verisi
          if echo "$ISSUE_BODY" | grep -q '<!-- EVENT_JSON_DATA_BASE64:'; then
            echo "📦 EVENT JSON verisi bulundu"
            BASE64_DATA=$(echo "$ISSUE_BODY" | grep -o '<!-- EVENT_JSON_DATA_BASE64:[^>]*' | sed 's/<!-- EVENT_JSON_DATA_BASE64://')
            JSON_DATA=$(echo "$BASE64_DATA" | base64 -d)
            OPERATION_TYPE="event"
          
          # DELETE JSON verisi  
          elif echo "$ISSUE_BODY" | grep -q '<!-- DELETE_JSON_DATA_BASE64:'; then
            echo "🗑️ DELETE JSON verisi bulundu"
            BASE64_DATA=$(echo "$ISSUE_BODY" | grep -o '<!-- DELETE_JSON_DATA_BASE64:[^>]*' | sed 's/<!-- DELETE_JSON_DATA_BASE64://')
            JSON_DATA=$(echo "$BASE64_DATA" | base64 -d)
            OPERATION_TYPE="delete"
          
          else
            echo "❌ JSON verisi bulunamadı"
            echo "OPERATION_TYPE=unknown" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "📄 JSON Data: $JSON_DATA"
          
          # jq ile parse et
          TITLE=$(echo "$JSON_DATA" | jq -r '.title // ""')
          DATE=$(echo "$JSON_DATA" | jq -r '.date // ""')
          TIME=$(echo "$JSON_DATA" | jq -r '.time // ""')
          CATEGORY=$(echo "$JSON_DATA" | jq -r '.category // ""')
          DESCRIPTION=$(echo "$JSON_DATA" | jq -r '.description // ""')
          SOURCES=$(echo "$JSON_DATA" | jq -r '.sources // ""')
          TYPE=$(echo "$JSON_DATA" | jq -r '.type // ""')
          ORIGINAL_FILENAME=$(echo "$JSON_DATA" | jq -r '.original_filename // ""')
          FILENAME_TO_DELETE=$(echo "$JSON_DATA" | jq -r '.filename // ""')
          
          # Varsayılan değerler
          TITLE="${TITLE:-Başlıksız Olay}"
          DATE="${DATE:-$(date +%Y-%m-%d)}"
          CATEGORY="${CATEGORY:-diğer}"
          DESCRIPTION="${DESCRIPTION:-Açıklama eklenmedi}"
          SOURCES="${SOURCES:-}"
          
          # Dosya adı oluştur
          if [[ "$OPERATION_TYPE" == "event" ]]; then
            if [[ "$TYPE" == "edit" && -n "$ORIGINAL_FILENAME" ]]; then
              FILENAME="$ORIGINAL_FILENAME"
              echo "📝 Düzenleme işlemi: $FILENAME"
            else
              SLUG=$(echo "$TITLE" | tr '[:upper:]' '[:lower:]' | \
                     sed -e 's/ç/c/g' -e 's/ğ/g/g' -e 's/ı/i/g' \
                         -e 's/ö/o/g' -e 's/ş/s/g' -e 's/ü/u/g' \
                         -e 's/Ç/c/g' -e 's/Ğ/g/g' -e 's/I/i/g' \
                         -e 's/İ/i/g' -e 's/Ö/o/g' -e 's/Ş/s/g' \
                         -e 's/Ü/u/g' | \
                     sed -e 's/[^a-z0-9]/-/g' -e 's/--*/-/g' -e 's/^-//' -e 's/-$//')
              FILENAME="${DATE}-${SLUG}.md"
              echo "🆕 Yeni olay: $FILENAME"
            fi
          elif [[ "$OPERATION_TYPE" == "delete" ]]; then
            FILENAME="$FILENAME_TO_DELETE"
            echo "🗑️ Silinecek dosya: $FILENAME"
          fi
          
          # GitHub Output'a yaz
          echo "OPERATION_TYPE=$OPERATION_TYPE" >> $GITHUB_OUTPUT
          echo "title=$TITLE" >> $GITHUB_OUTPUT
          echo "date=$DATE" >> $GITHUB_OUTPUT
          echo "time=$TIME" >> $GITHUB_OUTPUT
          echo "category=$CATEGORY" >> $GITHUB_OUTPUT
          echo "description<<EOF" >> $GITHUB_OUTPUT
          echo "$DESCRIPTION" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "sources<<EOF" >> $GITHUB_OUTPUT
          echo "$SOURCES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "filename=$FILENAME" >> $GITHUB_OUTPUT
          echo "original_filename=$ORIGINAL_FILENAME" >> $GITHUB_OUTPUT
          echo "type=$TYPE" >> $GITHUB_OUTPUT
          
          echo "=== PARSED VALUES ==="
          echo "OPERATION_TYPE: $OPERATION_TYPE"
          echo "TITLE: $TITLE"
          echo "DATE: $DATE"
          echo "CATEGORY: $CATEGORY"
          echo "FILENAME: $FILENAME"
          echo "TYPE: $TYPE"
          echo "===================="

      - name: Create or update event file
        if: steps.parse_json.outputs.OPERATION_TYPE == 'event'
        run: |
          mkdir -p events/data
          
          echo "Dosya işleniyor: events/data/${{ steps.parse_json.outputs.filename }}"
          
          cat > "events/data/${{ steps.parse_json.outputs.filename }}" << MDEOF
          ---
          title: "${{ steps.parse_json.outputs.title }}"
          date: "${{ steps.parse_json.outputs.date }}"
          time: "${{ steps.parse_json.outputs.time }}"
          category: "${{ steps.parse_json.outputs.category }}"
          ---

          ${{ steps.parse_json.outputs.description }}

          ## Kaynaklar
          ${{ steps.parse_json.outputs.sources }}
          MDEOF
          
          echo "✅ Dosya oluşturuldu/güncellendi: events/data/${{ steps.parse_json.outputs.filename }}"
          echo "--- DOSYA İÇERİĞİ ---"
          cat "events/data/${{ steps.parse_json.outputs.filename }}"
          echo "--- SON ---"

      - name: Delete event file
        if: steps.parse_json.outputs.OPERATION_TYPE == 'delete'
        run: |
          echo "Silinecek dosya: events/data/${{ steps.parse_json.outputs.filename }}"
          
          if [ -f "events/data/${{ steps.parse_json.outputs.filename }}" ]; then
            rm "events/data/${{ steps.parse_json.outputs.filename }}"
            echo "✅ Dosya silindi: events/data/${{ steps.parse_json.outputs.filename }}"
          else
            echo "❌ Dosya bulunamadı: events/data/${{ steps.parse_json.outputs.filename }}"
            exit 1
          fi

      - name: Create Pull Request for Edit Event
        if: steps.parse_json.outputs.OPERATION_TYPE == 'event' && steps.parse_json.outputs.type == 'edit'
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "📝 Olay güncelle: ${{ steps.parse_json.outputs.title }}"
          branch: update-event-${{ github.event.issue.number }}
          delete-branch: true
          title: "📝 Olay Güncelleme: ${{ steps.parse_json.outputs.title }}"
          body: |
            ## 📅 Olay Güncellendi
            
            **Issue:** #${{ github.event.issue.number }}
            **Dosya:** `${{ steps.parse_json.outputs.filename }}`
            **Tarih:** ${{ steps.parse_json.outputs.date }}
            **Kategori:** ${{ steps.parse_json.outputs.category }}
            
            ---
            Bu PR, issue #${{ github.event.issue.number }} (kapatıldığı için) tarafından otomatik olarak oluşturuldu.
            
            **Lütfen "Merge pull request" butonuna basarak onaylayın.**
          labels: |
            event
            automated
            update

      - name: Create Pull Request for New Event
        if: steps.parse_json.outputs.OPERATION_TYPE == 'event' && steps.parse_json.outputs.type != 'edit'
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "✨ Yeni olay ekle: ${{ steps.parse_json.outputs.title }}"
          branch: new-event-${{ github.event.issue.number }}
          delete-branch: true
          title: "✨ Yeni Olay: ${{ steps.parse_json.outputs.title }}"
          body: |
            ## 📅 Olay Eklendi
            
            **Issue:** #${{ github.event.issue.number }}
            **Dosya:** `${{ steps.parse_json.outputs.filename }}`
            **Tarih:** ${{ steps.parse_json.outputs.date }}
            **Kategori:** ${{ steps.parse_json.outputs.category }}
            
            ---
            Bu PR, issue #${{ github.event.issue.number }} (kapatıldığı için) tarafından otomatik olarak oluşturuldu.
            
            **Lütfen "Merge pull request" butonuna basarak onaylayın.**
          labels: |
            event
            automated
            new

      - name: Create Pull Request for Delete
        if: steps.parse_json.outputs.OPERATION_TYPE == 'delete'
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "🗑️ Olay sil: ${{ steps.parse_json.outputs.filename }}"
          branch: delete-event-${{ github.event.issue.number }}
          delete-branch: true
          title: "🗑️ Olay Sil: ${{ steps.parse_json.outputs.filename }}"
          body: |
            ## ⛔ Olay Silme İsteği
            
            **Issue:** #${{ github.event.issue.number }}
            **Silinen Dosya:** `${{ steps.parse_json.outputs.filename }}`
            
            ---
            ⚠️ **DİKKAT:** Bu işlem geri alınamaz!
            
            Bu PR, issue #${{ github.event.issue.number }} (kapatıldığı için) tarafından otomatik olarak oluşturuldu.
            
            **Lütfen "Merge pull request" butonuna basarak silme işlemini onaylayın.**
          labels: |
            event
            deletion
            automated

      - name: Comment on issue
        uses: actions/github-script@v7
        with:
          script: |
            const operationType = '${{ steps.parse_json.outputs.OPERATION_TYPE }}';
            const eventType = '${{ steps.parse_json.outputs.type }}';
            
            let prNumber;
            let message;
            
            if (operationType === 'event') {
              // Event PR'ları için - hangi PR oluştuysa onu al
              const editPR = '${{ steps.create-edit-pr.outputs.pull-request-number }}';
              const newPR = '${{ steps.create-new-pr.outputs.pull-request-number }}';
              prNumber = editPR || newPR;
              
              if (eventType === 'edit') {
                message = `✅ Issue kapatıldı. Olay güncelleme için Pull Request oluşturuldu!\n\n🔗 **PR:** #${prNumber}\n📁 **Dosya:** \`${{ steps.parse_json.outputs.filename }}\``;
              } else {
                message = `✅ Issue kapatıldı. Yeni olay için Pull Request oluşturuldu!\n\n🔗 **PR:** #${prNumber}\n📁 **Dosya:** \`${{ steps.parse_json.outputs.filename }}\``;
              }
            } else {
              // Delete PR'ı için
              prNumber = '${{ steps.create-delete-pr.outputs.pull-request-number }}';
              message = `✅ Issue kapatıldı. Silme işlemi için Pull Request oluşturuldu!\n\n🔗 **PR:** #${prNumber}\n🗑️ **Silinen Dosya:** \`${{ steps.parse_json.outputs.filename }}\``;
            }

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });